extends layout.pug

block content
    .jumbotron
        .container.text-center
            h1 Cake Shop
            p Fat and Sweet

    .container
        .row
            .col-xs-12
                a(href="#") Home
                |  / Registration

    .container
        .row
            .col-xs-12.text-center
                .login-form
                    form(action="#", method="post")
                        h2.text-center Registration
                        .form-group
                            input.form-control(type="text", name="firstname", placeholder="First Name", required="required")
                        .form-group
                            input.form-control(type="text", name="lastname", placeholder="Last Name", required="required")
                        .form-group
                            input.form-control(type="text", name="username", placeholder="Username", required="required")
                        .form-group
                            input.form-control(type="email", name="email", placeholder="Email", required="required")
                        .form-group
                            input.form-control(type="tel", name="phone", placeholder="Phone", required="required")
                        .form-group
                            input.form-control(type="password", name="password", placeholder="Password", required="required")
                        .form-group
                            input.form-control(type="password", name="confirm_password", placeholder="Confirm Password", required="required")
                        .form-group
                            button.btn.btn-primary.btn-block(type="submit") Register
